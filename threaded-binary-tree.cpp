   // if the left & right pointer  of any node is NULL
   //     in single threaded binary tree is the right node is null  then it store the address of the inorder successor
   //
   //     In double threaded  binary tree   if the left pointer is NULL then its store the  address of the  inorder pred successor
   //
